cls() # clear the log # -------------------# defining a functiondef deleteMissing(data):    newdata= list()    missing= 0    for dat in data:        if dat != None:            newdata.append(dat)        else:            missing+= 1    return (missing,newdata)#-----------------------def getUsedCols(malla= grid):	colNumber= list()	colString= list()	for col in range(malla.GetNumberCols()):		if malla.GetCellValue(0,col) != u'':			colNumber.append(col)			colString.append(malla.GetColLabelValue(col))	return (colNumber, colString)def getCol(colNumber,malla= grid):	result = list()	if not (colNumber in getUsedCols(malla)[0]):		return result	else:		for row in range(malla.GetNumberRows()):			result.append(malla.GetCellValue(row, colNumber ))	return deleteMissing(result)if 0:	plt= plot(None,			'plotLine',			data2plot=((range(10),range(10),'y=x'),				(range(10),[x*2 for x in range(10)],'y=2*x'))			)	plt.Show()	plt3= plot(None,			'plotBar',			data2plot=((range(5),'bar'),)			)	plt3.Show()	plt5= plot(None,			'boxPlot',			data2plot=(range(100),						[x*2 for x in range(10)],						[1,2,3,4,5,6,7,8,15])			)	plt5.Show()	plt6= plot(None,			'plotBarH',			data2plot=((range(10),'barH'),)			)	plt6.Show()	dic= {'Title':'titulo'}(colNumbers, colStrings) = getUsedCols()def makeStruct(colStrings):	str1 = list()	group = lambda x,y: (x,y)	bt1 = group('StaticText', ('Seleccione la columna para analizar',))	bt2 = group('Choice', (colStrings,))	str1.append( [bt1, ] )	str1.append( [bt2, ] )	return str1structura= makeStruct(colStrings)dic={'title','noname'}dlg = dialog(settings= dic, struct= structura)if dlg.ShowModal() == OK:	values= dlg.GetValue()dlg.Destroy()values= values[0]for colnumber, value in zip(colNumbers, colStrings):	if values == value:		breakshow('selected value')show(colnumber)show(grid.GetNumberRows())#(missing,data) = deleteMissing(grid.getNumByCol(colnumber))show('se leyo exitosamente')