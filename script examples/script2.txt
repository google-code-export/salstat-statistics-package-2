cls() # clear the log # show a simple calculationshow(2+5-2*(3))# defining variablesperas = 5manzanas = 8totalFrutas = peras + manzanas# mostrar las variableshow(totalFrutas)# definir un diccionariofrutas= dict()frutas['peras'] = 5frutas['manzanas'] = 8frutas['melones' ] = 5show("total de frutas = %5.0f"%sum(frutas.values()))# -------------------# defining a functiondef deleteMissing(data):    newdata= list()    missing= 0    for dat in data:        if dat != None:            newdata.append(dat)        else:            missing+= 1    return (missing,newdata)#-----------------------#--------------------# using the function above created(missing,data) = deleteMissing(grid.getNumByCol(0))#-------------------#--------------------# show a calculation in the logshow("Missing data %5.0f"%missing,	data)#----------------------#---------------------# using an statistic function,# the show a message in the logtry:	mean = stats.mean(data)	show("mean =  %5.4f"%mean)except ZeroDivisionError:	show("Error: No se identifico informacion\ncontenida en la primer columna de la malla")#---------------------#--------------------------# some examples of the ploting systemplt= plot(None,        'plotLine',        data2plot=((range(10),range(10),'y=x'),            (range(10),[x*2 for x in range(10)],'y=2*x'))        )plt.Show()plt2= plot(None,        'plotScatter',        data2plot=((range(10),range(10),'y=x'),            (range(10),[x*2 for x in range(10)],'y=2*x'))        )plt2.Show()plt3= plot(None,        'plotBar',        data2plot=((range(5),'bar'),)        )plt3.Show()plt4= plot(None,        'plotLinRegress',        data2plot=(range(10),[x*2 for x in range(10)],'y=2*x')        )plt4.Show()plt5= plot(None,        'boxPlot',        data2plot=(range(100),[x*2 for x in range(10)],[1,2,3,4,5,6,7,8,15])        )plt5.Show()plt6= plot(None,        'plotBarH',        data2plot=((range(10),'barH'),)        )plt6.Show()